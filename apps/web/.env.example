# Your context.dev API key.
# Server-only — NEVER prefix with NEXT_PUBLIC_; it must not be exposed to the client.
# Get a key at: https://context.dev
CONTEXTDEV_API_KEY=

# Optional: durable cache for on-demand landscapes (Vercel KV / Upstash REST). Share links survive cold starts.
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
