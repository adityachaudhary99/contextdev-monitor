import "./globals.css";
export const metadata = { title: "Context.dev Intelligence Monitor", description: "Track competitor pricing changes with evidence-linked diffs." };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (<html lang="en"><body>{children}</body></html>);
}
